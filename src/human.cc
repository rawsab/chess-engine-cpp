#include "human.h"
#include "types.h"
#include "player.h"
#include <iostream>

Human::Human(Color c) : Player(c) {
    // Constructor implementation
}

Move Human::getMove() {
}

Human::~Human() {
    // Destructor implementation (if needed)
}
